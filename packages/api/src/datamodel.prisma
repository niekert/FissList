type User {
  id: ID! @unique
  name: String!
}

type Party {
  id: ID! @unique
  name: String!
  activeTrackIndex: Int
  createdAt: DateTime!
  updatedAt: DateTime!
  ownerUserId: String!
  playlistId: String!
  trackIds: [String!]!
  partyMemberIds: [String!]
}

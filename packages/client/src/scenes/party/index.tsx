import * as React from 'react';
import { Title } from 'components/Typography';

interface IProps {
  path?: string;
}

export default function Party(props: IProps) {
  return <Title>New Party!</Title>;
}

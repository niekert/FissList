import * as React from 'react';
import { Player_player_devices } from './__generated__/Player';

function DeviceSelect({ devices }: { devices: Player_player_devices[] }) {
  return <div />;
}

export default DeviceSelect;
